# План реализации: [ФУНКЦИЯ]

**Ветка**: `[###-feature-name]` | **Дата**: [DATE] | **Спецификация**: [ссылка]
**Входные данные**: Спецификация функции из `/specs/[###-feature-name]/spec.md`

**Примечание**: Этот шаблон заполняется командой `/speckit.plan`. См. `.specify/templates/commands/plan.md` для рабочего процесса выполнения.

## Резюме

[Выдержка из спецификации функции: основное требование + технический подход из исследования]

## Технический контекст

<!--
  ТРЕБУЕТСЯ ДЕЙСТВИЕ: Замените содержимое этого раздела техническими деталями
  для проекта. Структура здесь представлена в консультативных целях для руководства
  процессом итерации.
-->

**Язык/Версия**: [например, Python 3.11, Swift 5.9, Rust 1.75 или ТРЕБУЕТСЯ УТОЧНЕНИЕ]
**Основные зависимости**: [например, FastAPI, UIKit, LLVM или ТРЕБУЕТСЯ УТОЧНЕНИЕ]
**Хранилище**: [если применимо, например, PostgreSQL, CoreData, файлы или Н/Д]
**Тестирование**: [например, pytest, XCTest, cargo test или ТРЕБУЕТСЯ УТОЧНЕНИЕ]
**Целевая платформа**: [например, сервер Linux, iOS 15+, WASM или ТРЕБУЕТСЯ УТОЧНЕНИЕ]
**Тип проекта**: [один/веб/мобильный - определяет структуру исходного кода]
**Цели производительности**: [специфичные для домена, например, 1000 запросов/с, 10k строк/сек, 60 fps или ТРЕБУЕТСЯ УТОЧНЕНИЕ]
**Ограничения**: [специфичные для домена, например, <200мс p95, <100МБ памяти, автономная работа или ТРЕБУЕТСЯ УТОЧНЕНИЕ]
**Масштаб/Объем**: [специфичные для домена, например, 10k пользователей, 1M LOC, 50 экранов или ТРЕБУЕТСЯ УТОЧНЕНИЕ]

## Проверка конституции

*ПОРÓГ: Должен пройти до исследования фазы 0. Повторная проверка после проектирования фазы 1.*

[Пороги определяются на основе файла конституции]

## Структура проекта

### Документация (эта функция)

```text
specs/[###-feature]/
├── plan.md              # Этот файл (вывод команды /speckit.plan)
├── research.md          # Вывод фазы 0 (команда /speckit.plan)
├── research/            # Сложное исследование (если необходимо - для глубоких исследовательских задач)
├── data-model.md        # Вывод фазы 1 (команда /speckit.plan)
├── quickstart.md        # Вывод фазы 1 (команда /speckit.plan)
├── contracts/           # Вывод фазы 1 (команда /speckit.plan)
└── tasks.md             # Вывод фазы 2 (команда /speckit.tasks - НЕ создается командой /speckit.plan)
```

### Исходный код (корень репозитория)
<!--
  ТРЕБУЕТСЯ ДЕЙСТВИЕ: Замените дерево заполнителей ниже конкретной структурой
  для этой функции. Удалите неиспользуемые варианты и расширьте выбранную структуру
  реальными путями (например, apps/admin, packages/something). Доставленный план должен
  не включать метки вариантов.
-->

```text
# [УДАЛИТЬ ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 1: Один проект (ПО УМОЛЧАНИЮ)
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [УДАЛИТЬ ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 2: Веб-приложение (когда обнаружены "frontend" + "backend")
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [УДАЛИТЬ ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 3: Мобильное приложение + API (когда обнаружены "iOS/Android")
api/
└── [то же, что и backend выше]

ios/ или android/
└── [специфичная для платформы структура: модули функций, UI потоки, платформенные тесты]
```

**Решение о структуре**: [Документируйте выбранную структуру и ссылайтесь на реальные
каталоги, зафиксированные выше]

## Отслеживание сложности

> **Заполняйте ТОЛЬКО если проверка конституции выявила нарушения, которые должны быть обоснованы**

| Нарушение | Зачем нужно | Простая альтернатива отклонена потому что |
|-----------|------------|-------------------------------------|
| [например, 4-й проект] | [текущая потребность] | [почему 3 проекта недостаточно] |
| [например, паттерн Repository] | [конкретная проблема] | [почему прямой доступ к БД недостаточен] |
